# Automation pipeline for continuous integration and QA environments
name: Run Ephemeral Tests

permissions:
  contents: write
  pages: write
  id-token: write

on:
  push:
    branches: [main, qa]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      custom_id:
        description: "Custom Identifier (optional, defaults to Run ID)"
        required: false
        type: string

env:
  TERRAFORM_VERSION: "1.14.4"
  PYTHON_VERSION: "3.11"
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: ${{ env.ACR_LOGIN_SERVER }}/ephemeral-qa-app:${{ inputs.custom_id || github.run_id }}

  deploy:
    name: Provision
    needs: build
    uses: ./.github/workflows/reusable/deploy.yml
    with:
      run_id: ${{ inputs.custom_id || github.run_id }}
      environment: "qa"
      container_image: ${{ secrets.ACR_LOGIN_SERVER }}/ephemeral-qa-app:${{ inputs.custom_id || github.run_id }}
      terraform_version: "1.14.4"
    secrets: inherit

  test:
    name: Regression
    needs: deploy
    uses: ./.github/workflows/reusable/test.yml
    with:
      target_url: ${{ needs.deploy.outputs.app_url }}
      python_version: "3.11"
      execution_type: "CI/CD Pipeline"
      image_tag: ${{ inputs.custom_id || github.run_id }}

  cleanup:
    name: Destroy
    needs: [deploy, test]
    if: always() && needs.deploy.result == 'success'
    uses: ./.github/workflows/reusable/cleanup.yml
    with:
      run_id: ${{ inputs.custom_id || github.run_id }}
      environment: "qa"
      container_image: ${{ secrets.ACR_LOGIN_SERVER }}/ephemeral-qa-app:${{ inputs.custom_id || github.run_id }}
      terraform_version: "1.14.4"
    secrets: inherit
